<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oams">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentAttendanceActivity"></activity>
        <activity android:name=".login.LoginTabActivity" />
        <activity android:name=".teacher_activitys.Number_stdActivity" />
        <activity android:name=".teacher_activitys.ShortageActivity" />
        <activity android:name=".teacher_activitys.AddAttendanceActivity" />
        <activity android:name=".admin_activitys.StudentAddActivity" />
        <activity android:name=".admin_activitys.AddTeacherActivity" />
        <activity android:name=".admin_activitys.HomeAdminActivity" />
        <activity
            android:name=".teacher_activitys.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login.StudentActivity" />
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>